# Build artifacts
build/
*.wasm
*.o
*.a
*.so

# Video test harness
test/video_harness/video_harness

# Docker (local overrides only - .env is committed)
.env.local

# Node.js
node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Test output
test/output.png
test/*.bin
test/*.json
# Allow sanitized proto snapshots (used for PNG/video tests)
# The .json is human-verifiable; .bin is generated from it via:
#   cd tools/snapshot && npm install && npm run json2proto
!test/proto_snapshot.json
!test/proto_snapshot.bin

# Video test outputs (Phase 3)
test/output/
test/synthetic_states/
*.mp4
*.mkv
*.webm

# Snapshot output (PNG renders from protobuf snapshots)
snapshot/*
!snapshot/.gitkeep

# Output directory for generated files
output/
*.png
# Allow navball skin PNGs to be committed
!resources/navball_skins/*.png

# Editor
*.swp
*.swo
*~
.DS_Store

# CLion/JetBrains IDE - ignore entire directory
.idea/

# Compilation database temp files (generated by bear)
prev_compile_commands.json
temp_compile_commands.json

# Dev Container
.devcontainer/.env
.devcontainer/.env.local

# Compiled TypeScript
tools/snapshot/dist/
tools/snapshot/build/
*.js
*.js.map
!tools/snapshot/package.json

# User-specific hook files
tools/snapshot/src/hooks.ts

# Temporary files
*.tmp
*.temp
test/video_harness/x264_2pass.log*

# Package output
dist/*.tar
dist/*.tar.gz

# Chat files
chat.txt
